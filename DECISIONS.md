# DECISIONS.md — 설계 의사결정 로그

> 발표 시 "왜 이렇게 설계했는가"를 설명하기 위한 기록.  
> 방향 결정이 생길 때마다 즉시 추가.

---

## 형식

```
## [날짜] 결정 제목
**결정**: 무엇을 선택했는가
**이유**: 왜 이 선택을 했는가
**고려했던 대안**: 무엇을 버렸는가, 왜
**영향**: 이 결정이 이후 무엇을 바꾸는가
```

---

## [2026-06-20] 하이브리드 검색 전략 채택

**결정**: 단순 벡터 유사도 검색 대신 메타데이터 필터링 + 벡터 유사도의 하이브리드 스코어링 채택  
**이유**: 제안서는 산업군·프로젝트 유형 같은 구조화된 속성이 검색 적합성에 결정적. 순수 시맨틱 검색은 "자동차 XR 제안"과 "가전 XR 제안"을 잘못 혼합할 수 있음  
**고려했던 대안**: 순수 벡터 검색만 → 메타데이터 기반 유사성을 반영 못함 / 순수 키워드 검색만 → 표현 다양성(동의어, 맥락)을 포착 못함  
**영향**: Retriever 모듈에서 두 점수(α=0.4 메타데이터, 0.6 시맨틱)를 결합하는 하이브리드 스코어링 함수 구현 필요

---

## [2026-06-20] 섹션 단위 청킹 채택

**결정**: 문서 전체를 단일 청크로 쓰지 않고, 섹션 단위(배경분석, 솔루션개요 등)로 청킹  
**이유**: 제안서는 명확한 섹션 구조를 가지며, 초안 생성 시 "이 섹션은 A 문서 참조, 저 섹션은 B 문서 참조"처럼 세밀한 재사용이 가능해짐  
**고려했던 대안**: 고정 토큰 수 청킹(512/1024 tokens) → 섹션 경계를 무시하여 의미 단위 훼손  
**영향**: Ingestor가 섹션 경계를 인식하는 파싱 로직 필요. Mock 데이터도 섹션 구분 포함하여 설계

---

## [2026-06-20] LLM 2단계 호출 구조 결정

**결정**: LLM 호출을 1차(Request Analyzer: 입력 구조화)와 2차(Draft Generator: 섹션별 생성)로 분리  
**이유**: 단일 프롬프트로 "입력 파싱 + 11개 슬라이드 생성"을 동시에 요청하면 컨텍스트 혼재로 품질 저하. 역할 분리로 각 LLM 호출의 집중도 향상  
**고려했던 대안**: 단일 거대 프롬프트 → 품질 불안정, 디버깅 어려움  
**영향**: API 호출 횟수 증가(1 + 섹션 수)이나, 섹션별 품질 제어 및 재생성 가능성 확보

---

---

## [2026-06-20] 기술 스택 확정 — Option B (LangChain + ChromaDB + OpenAI + Claude + Streamlit)

**결정**: LangChain 기반 RAG + ChromaDB(로컬) + OpenAI text-embedding-3-small + Claude claude-sonnet-4-6 + Streamlit  
**이유**: 외부 API 사용 허용 확인 → 임베딩 품질 최우선. Streamlit으로 데모 시연 품질 확보. ChromaDB만 사용하여 외부 Vector DB 서비스 의존성 제거  
**고려했던 대안**: Option A (로컬 임베딩) → 보안 요건 없으므로 불필요. Option C (LangChain 없는 커스텀) → LangChain 쓰는 게 더 빠르고 완성도 높음  
**영향**: requirements.txt에 langchain, chromadb, openai, anthropic, streamlit, python-pptx 포함

---

## [2026-06-20] PPT 출력 전략 — 소스 PPTX 복사 + 텍스트 교체

**결정**: 별도 디자인 템플릿 없이 top-1 검색 결과 PPTX를 복사한 뒤 python-pptx로 텍스트만 교체  
**이유**: 소스 21개 문서가 동일한 올림플래닛 디자인 언어를 공유하므로 별도 템플릿 불필요. 복사 방식으로 배경·레이아웃·폰트 완전 보존  
**고려했던 대안**: 빈 PPTX 새로 생성 → 디자인 재현 공수 크고 결과물 품질 저하  
**영향**: PPT Assembler가 shutil.copy 후 python-pptx로 텍스트 프레임 찾아 교체하는 구조. 텍스트 길이 제한(헤드라인 ≤40자) 필수

---

## [2026-06-20] 실제 데이터 확인 — CSV 컬럼명·PPTX 구조 반영

**결정**: 초안 SPEC의 가상 필드명 대신 실제 CSV 컬럼명(한국어) 사용. 프로젝트 유형 실제 값 6종으로 확정  
**이유**: CSV 파일 직접 확인 결과 컬럼 구조가 설계 초안과 다름(client_scale 없음, project_type 값 다름). 실데이터 기준으로 맞춤  
**고려했던 대안**: 한국어 컬럼명을 영어로 변환 → 불필요한 매핑 추가, 실수 가능성  
**영향**: Ingestor가 CSV 파이프(|) 구분자 파싱 필수. 확장 레퍼런스(S4) 산업군을 fallback 풀로 처리

<!-- 이후 결정 사항은 아래에 추가 -->
